# Red/blue ultra finalization QA — 2026-08-30

Status: **accepted with revision for the integrated source candidate; exact
manifest sealing, clean-tip verification, upstream readback, and external ZIP
attestation remain terminal**

## Outcome first

The final ultra pass found useful work in all three challenged areas and closed
every reproduced P0/P1. It did not justify a new public route, seventh family,
dashboard, score, security subsystem, empirical run, Echo rewrite, archive
rewrite, or Signal Foundry reseal.

- The public surface now has readable Echo link states, exact document-level
  navigation, honest repository-only references, focus-neutral disclosure
  reveal, defense-in-depth review indexing, and a human-function-first short
  heading.
- The machine receipt now reconciles memory use with selected influence,
  rejects duplicate/ambiguous control data, preserves unknowns on non-answer
  routes, and makes the general payload data/control boundary explicit without
  claiming a security result.
- The owner handoff now has a complete deterministic exact-commit package path,
  strict copied-extraction verification, truthful Git/extracted modes,
  race-safe no-overwrite behavior, and explicit publication/rights gates.

The owner intent, six-family identity, essay body, Echo firebreak, immutable
archive, and sealed Signal Foundry packet were preserved.

## Team topology and review method

The integrator froze a correction specification from baseline
`76ade6e2c255151e32ddd9cbb3d4650cf46570d1`, then ran three bounded lanes:

1. a public-surface/accessibility implementation lane plus an independent
   semantic-navigation recheck;
2. a hostile machine-receipt contract lane; and
3. a read-only owner-package blue team that repeatedly attacked the live
   successor while the integrator applied reproduced fixes.

The integrator independently reran focused source suites, rendered the current
site in the in-app browser, inspected the six-page PDF, and reconciled every
accepted, deferred, or rejected finding into D-050 and the controlled
disposition ledger. Advisory detail was never treated as authority merely
because it was detailed.

## Rendered browser evidence

The review site was exercised at 1440×900, 390×844, and 320×800; the public
mode was served independently at 1440×900. Observed source/render facts:

- Home, Read, and Research each had one `h1`, a main landmark, and no document
  horizontal overflow at all tested widths.
- The 320-pixel Home composition remained readable; principal navigation
  wrapped without clipping, the More menu expanded with all six secondary
  routes, and its 300-pixel menu stayed inside the 320-pixel viewport.
- The Echo callout resolved to
  `research/index.html#echo-identity-document`; the exact target exists inside
  its disclosure. Normal browser-computed link color was `rgb(241, 189, 128)`
  on the purple callout. Keyboard focus produced `rgb(59, 48, 76)` on
  `rgb(255, 246, 221)` with a 3-pixel visible outline and `:focus-visible` true.
- Clicking the Echo link and direct-opening `#echo-status-document` opened the
  correct disclosure. The active element remained the initiating link or body
  as native navigation required; the runtime did not call `focus()`.
- Direct-opening `#mentor-handoff-document` revealed content nested inside its
  disclosure and did not create horizontal overflow.
- Review Home/Research and unreleased public Home/Research all rendered
  `noindex,nofollow`. Public mode removed local-owner-review and review-candidate
  chrome while retaining the fail-closed metadata state.

These are browser/source observations, not a physical-keyboard traversal,
supported screen-reader result, real OS/browser 200% zoom result, forced-colors
result, native print-preview result, hardware-touch result, or reader study.

## PDF evidence

The documented PDF producer was rerun with the bundled workspace runtime after
the required artifact-operation marker. The output remained byte-identical:

- SHA-256:
  `372b7b5fd3ad9a8838eb832b5c44ea83593622138a44b6c16c15b1688a52f25d`
- byte size: `18,373`
- pages: `6`
- semantic status: intentionally untagged visual companion; standalone HTML
  remains the semantic route

All six pages were rendered at 144 dpi with the bundled Poppler tools and
visually inspected. No clipping, overlap, missing glyph, or page-composition
defect was found. Because the regenerated bytes were identical, the PDF and
its existing producer checkpoint did not change.

## Focused deterministic evidence before sealing

The following source-level checks passed during convergence:

- locked owner-intent checksum;
- applied receipt/framework validator with permanent hostile mutations;
- JSON parse sweep for tracked receipt fixtures;
- canonical site review/public build and complete site/content/interaction
  suite;
- exact source-navigation/disclosure contract;
- structural site audit;
- publication-kit fail-closed and rights-boundary contract;
- Python compilation and shell syntax for owner-package controls;
- nine-test owner-package adversarial suite, including production exact-HEAD
  preflight;
- scoped and repository-wide `git diff --check`; and
- six-page PDF regeneration/hash/render inspection.

The bounded owner-review manifest is intentionally stale while covered source
bytes move. That failure is expected and must not be weakened. The complete
twelve-stage runner is intentionally deferred until the final manifest-only
commit produces an exact clean checkout.

## Residuals and nonclaims

The deeper package pass reproduced three additional P1s—the unbound copied
verifier, publication-race success, and a wrapper-prefixed archive-policy
misclassification that would block the real ZIP—and three P2 classes:
unrelated nested Git roots, a symlinked bounded manifest, and
detached/default-branch authoring. Each was fixed and mutation-covered. The
complete nine-test hostile package suite passes after those revisions, leaving
no reproduced P0/P1. Two P3
limitations remain explicit: the high-signal credential/private-key guard is
not a general secret detector, and complete production-tree preflight has a
bounded one-time local time/memory cost.

Manual owner/mentor comprehension and taste; a physical end-to-end keyboard
pass; supported screen reader; real zoom and forced colors; native print
preview; hardware touch; byline, canonical URL, social image and alternative
text; copyright/license and historical/social-image rights; destination and
live-link checks; and explicit merge/publication authorization remain open.

No merge, deployment, publication, Release, provider/model/study/participant
run, dataset acquisition, preregistration, outreach, or incremental spend was
performed. No validator, fixture, model review, local browser observation, or
package hash is represented as evidence that the framework improves answers.

## Terminal handoff rule

Freeze source and evidence, commit them, regenerate the bounded manifest from
the exact clean named non-default feature branch, commit the manifest alone,
push/read back the exact upstream tip, run the complete twelve-stage suite on
that clean tip, then build and independently verify the `--require-upstream`
owner ZIP outside the repository. Record remote and ZIP facts externally. Do
not rewrite this covered narrative after sealing merely to describe its own
later verification.
