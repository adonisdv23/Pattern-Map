# Pattern Map v16 public-and-transfer hardening QA

Status: **PRE-SEAL INTEGRATION PASS — FINAL REMOTE, CLAUDE, AND PACKAGE READBACKS PENDING**

Audit date: 2026-08-30

This record covers the bounded v16 hardening phase authorized after the
`4d2e11ceb26b36fd7428dc7de963c135802a8dea` owner-review baseline. It is
implementation and artifact evidence only. It is not a reader study, model
evaluation, accessibility certification, publication, deployment, or research
result.

## Governing boundary

The phase did not reopen the locked owner intent, rewrite the human essay,
rename or reorder the six families, collapse The Echo Problem into v16, or
create a new v17 thesis. The permanent artifact boundaries and authority order
remain controlling. No merge to `main`, deployment, publication, provider or
model study, participant activity, dataset acquisition, spend, preregistration,
or outreach was authorized or performed.

## Provenance

| Lane | Exact source head | Integrated checkpoint | Role |
| --- | --- | --- | --- |
| Phase 0 plan | `37c7c852648b5da25365725453960464552a46b8` | already on the integration branch | Locked the bounded public/transfer scope |
| Applied integrity | `93265692e95d56e35f8de68afcc435519419684b` | `28fe04a`, `d71cbbc`, `811e39e` | Stage 0, permission, evidence-reference, motion, and memory contracts |
| Public presentation | `361243c95050723f1693907f7446be5d690b9e58` | `63aceb7`, `b761146` | Shared-source public adapter, teaching reveal, and fail-closed release gate |
| Research/claims | `54bd0a7a11f4c072c8ceaab1a2abf7bc81a380cb` | `51643ad`, `d8f6f09` | Targeted current-source route and unrun narrow-wedge boundary |
| Initial convergence | advisory/source checkpoint `cbc89db45493fd1dcfd121af0d1da1393046a196` | `cbc89db` | First three-lane integrated review surface |
| Research correction | `ecf37ee64adfeb1847a5b6342d7550f7f5da6695` | `c3ab1d7` | Separates study modes and missingness axes; removes a false public self-link |
| Public URL correction | `c4a0f4c62e71cb2475f286f67ae0254a13f130a3` | `c0b006f` | Parsed canonical-base validation and URL-semantic route resolution |

These commits are implementation provenance, not independent evidence that the
framework works. Model and agent findings were dispositioned under the
repository authority order rather than accepted because they were detailed.

## Integrated outcomes and dispositions

### 1. Ordinary work remains ordinary

**Accepted with revision.** Stage 0 is terminal inside the copyable operating
brief. When no evidence-selection workflow is needed, the receipt contains
exactly supplied scope, assumptions, unchecked boundaries, and output. It does
not create evidence, comparison, disconfirmation, influence, outcome, or
learning bureaucracy.

### 2. Layered receipts fail closed without pretending to establish truth

**Accepted with revision.** Permission uses the distinct states `AUTHORIZED`,
`UNKNOWN`, `NOT_AUTHORIZED`, and `REVOKED`. A globally blocked receipt has empty
downstream collections. Comparison and disconfirmation claims resolve to
substantive linked records or bounded typed exceptions. Motion requires at
least two distinct authorized observations at two valid, distinct UTC instants
under one alignment key. Only accepted `CURRENT` and `AUTHORIZED` memory may
influence a later answer, and the synthetic correction fixture preserves one
linear hash-bound lineage.

These checks establish schema and validator behavior only. They do not prove
that a source is true, that permission is legally sufficient, that a runtime
store is immutable, that an agent complies, or that an answer improves.

### 3. Public presentation is an adapter over the same source

**Accepted with revision.** Review and public modes retain the same ten route
IDs, forty-four canonical source hashes, claim anchors, and F1–F6 tuple. Public
mode removes owner/package/repository chrome from the normal reading flow and
places the real short-version prose in the first Read viewport. One fixed
four-stage teaching reveal replaces its prior state rather than accumulating
claims. It has a static/no-script/print equivalent and makes no network call,
model call, score, automated decision, or observed-result claim.

Release metadata fails closed unless an explicit release build is paired with
valid absolute-HTTPS identity fields. Canonical bases reject credentials,
whitespace, queries, and fragments; route URLs are resolved with URL semantics
and preserve a valid project subpath. Ordinary public previews remain
`noindex,nofollow`. No byline, canonical public URL, social image, public link
recheck, deployment, or publication was selected.

### 4. Current research constrains the claims without taking over the thesis

**Accepted with revision.** The optional source route adds a targeted,
non-exhaustive 2025–2026 primary-source check. It supports a design/governance
synthesis and testable-agenda ceiling, not novelty or validation of a new
technical mechanism. The narrow-wedge memo separates generation studies from
fixed-answer interface studies: answer accuracy may be eligible only when an
arm actually generates an answer, while correction, acceptance, and burden are
the eligible outcomes for a frozen-answer interface comparison.

Observation, capture/process, access, permission, and currency are represented
as different axes. The initial unrun wedge holds access `AVAILABLE`, permission
`AUTHORIZED`, and currency `CURRENT` while varying observation and capture.
No first paper, provider, model, corpus, sample, study, or spend was selected.

### 5. Signal Foundry receives a selected operating packet, not the whole repo

**Accepted with revision.** The packet includes the human thesis, stable map,
operator and agent guidance, ordinary and memory templates, the frozen memory
anchor, distinct permission fixtures, bounded cases, and the two Signal
Foundry handoffs. It deliberately excludes the public adapter, public
standalone, visual captures, lane QA, and the future narrow-study memo because
those are owner-review materials rather than downstream implementation inputs.

Every retained relative Markdown link that points outside the packet is
classified as `archive`, `owner_review_only`, or `outside_selected_packet`.
Inline and reference-definition links are checked. New, stale, invalid, or
absent classified targets fail the build. Every declared owner-review exclusion
must exist at the sealed source commit and remain disjoint from the selected
payload. The generated controls tell a fresh-computer receiver to request any
missing tracked input rather than infer it.

The packet remains read-only context. Signal Foundry's tracked guidance and
contracts remain authoritative in its own checkout. The handoff recommends
testing the existing `OPERATOR_DECISION` plus `RATIONALE` seam before proposing
a new event type; `CONTEXT_DISPOSITION` remains conceptual and is not valid
against the current downstream schema.

### 6. PDF cover punctuation defect

**Accepted; corrected locally.** The canonical string was already correct, but
the 25-point ReportLab cover style exceeded the usable line width and rendered
the final punctuation as a detached `word .`. A cover-only 23-point style keeps
`word.` together without changing the hierarchy on pages two through six. The
regenerated six-page Letter PDF was rendered with Poppler at 144 DPI and all
six pages were inspected. No clipping, overlapping cards/tables, unreadable
glyphs, or broken footer was observed. The PDF is still deliberately untagged;
the standalone HTML remains the semantic accessibility route.

## Verification completed at the pre-seal checkpoint

The following provider-free checks pass in the integration worktree:

```text
(cd docs && shasum -a 256 -c OWNER_INTENT_V16.sha256)
python3 qa/editorial/validate_content_interface.py
python3 qa/applied/validate_framework.py
python3 qa/research/validate_research_boundaries.py
(cd site && npm run build && npm run check)
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v qa/handoff/test_portable_bundle.py
git diff --check
```

Observed results within those commands' stated scopes:

- locked owner intent: pass;
- exact content interface, six-family order, claim and no-results boundaries:
  pass;
- ordinary/layered, permission, reference, motion, and memory mutations: pass;
- targeted current-source and research-boundary validation: pass;
- ten review routes, ten public routes, and both standalone exports: pass;
- review/public source, route, claim, and family parity: pass;
- public metadata and explicit-release gate: pass;
- public prose-first Read and deterministic reveal: pass;
- Apply Stage 0 and all 108 planning combinations: pass;
- line-free Map, desktop term geometry, reader language, and CSS reachability:
  pass;
- portable packet regressions: 12/12 pass, including deterministic ZIPs,
  manifest coverage, embedded verification, all-payload marker scanning,
  optional local-input guards, operating-input/exclusion policy, and inline and
  reference-link classification;
- regenerated PDF: six pages, Letter size, unencrypted, no JavaScript; all six
  Poppler renders visually inspected.

## Evidence ceiling and manual residuals

Automated, headless-browser, synthetic DOM/CDP, computed-geometry,
print-emulation, and rendered-PDF checks do not establish human comprehension,
persuasion, framework effectiveness, model quality, or physical accessibility.
The following stay open for the owner/publication path:

- owner and mentor comprehension, voice, and aesthetic judgment;
- physical end-to-end keyboard traversal;
- supported VoiceOver or NVDA review;
- real browser/OS 200% zoom;
- real forced-colors inspection;
- native browser print preview;
- hardware-touch activation;
- publication-time byline, canonical URL, social image, external-link,
  Open Graph, and metadata verification.

## Remaining terminal gates

Before this record can be marked terminal, the orchestrator must:

1. commit the converged source and this evidence record;
2. regenerate the owner-review manifest from the exact committed bytes;
3. run the full twelve-stage verifier in a clean clone;
4. obtain and disposition one read-only independent Claude review of the exact
   integration checkpoint;
5. perform a final live local-browser inspection when the in-app browser
   control path is available, without substituting it for the manual gates;
6. push the authorized feature branch, open/read back a matching draft PR, and
   keep it unmerged;
7. build the cross-computer Signal Foundry ZIP from the exact pushed commit,
   then verify its sidecar, archive structure, embedded verifier, manifest,
   source-path/key-marker scan, branch/commit/PR metadata, and copied-location
   fresh-computer flow.

Until those gates are recorded, this is a passing pre-seal checkpoint rather
than a terminal owner handoff.
