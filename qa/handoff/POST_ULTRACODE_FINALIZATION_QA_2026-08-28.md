# Post-Ultracode finalization QA

Status: **SOURCE CONTRACTS ACCEPTED; FINAL PACKAGE SEAL AND OWNER/MANUAL GATES REMAIN**

Audit date: 2026-08-28

Reviewed baseline:
`d5b3431a02d5ffc4d084baa278d7a18fd98a71ad`

Owner-supplied baseline packet:
`PATTERN_MAP_V16_SIGNAL_FOUNDRY_PORTABLE_2026-08-27_d5b3431.zip`

Verified baseline packet SHA-256:
`b806b8b143fce5a003c1040b2aeca69804b3695e55d6a384211f89aa1f62caae`

Accepted post-Ultracode source checkpoint:
`c86e53764f6e33b62097f0424125b5922441ce58`

This record dispositions Claude Ultracode, Luna Max, and primary-integrator
findings as advisory evidence under the repository authority order. It does
not treat model detail as owner intent, empirical evidence, or authority. No
model or provider was called by the repository harness, no study ran, and no
Signal Foundry state was changed.

## What was deliberately preserved

- The human problem remains the opening: generic output can begin with generic
  upstream noticing, retrieval, comparison, context, assumption, and memory
  choices.
- All six original families remain the complete map: peripheral signal; source
  weighing; velocity/motion; absence + memory; structured patterns; and the
  learning loop.
- The Echo Problem remains separate, subordinate, unrun, and without results.
- Signal Foundry remains an illustration and downstream design target, not
  validation or an implemented integration.
- The routed ten-page site remains the primary local reader experience. No
  wholesale visual or manuscript rewrite was justified.
- Ordinary work remains allowed to stay ordinary. No new universal ledger,
  score, classifier, event stream, or mandatory project-adoption artifact was
  introduced.

## Accepted portable-package findings

| ID | Finding | Disposition and correction | Verification boundary |
| --- | --- | --- | --- |
| UC-PKG-01 | The handoff confused the fixed `874a0a8` content checkpoint with a moving branch/package head | **Accepted with revision.** Human summaries identify `874a0a8` as the content checkpoint; machine data uses `head: null` with resolve-at-use instructions. Generated packet metadata is the sole sealed source-commit record. | Tests parse the correct JSON fence and reject a static forty-hex head. |
| UC-PKG-02 | Selected Markdown links can point outside the portable subset | **Accepted with revision.** Generated START_HERE and COPYABLE_PROMPT state that the packet is a selected subset and require the exact missing file or repository state instead of inference. Historical files were not rewritten and archive closure was not chased recursively. | This is a documented subset boundary, not a claim that every repository link is portable. |
| UC-PKG-03 | The verifier scanned only recognized text suffixes | **Accepted with revision.** Builder and embedded verifier now scan every payload for high-signal private-key bytes and scan printable ASCII runs for source-machine paths. | Fully resealed PNG/PDF attacks fail; benign resealed binary controls pass. This is marker hygiene, not general malware detection. |
| UC-PKG-04 | A local-only Signal Foundry audit and untracked guidance files were presented as required on another computer | **Accepted with revision.** `4a6ed78`, local `AGENTS.md`/`CLAUDE.md`, and a local `.gitignore` delta are optional local evidence. Required tracked inputs still fail closed; optional local absence records `ABSENT/UNVERIFIED` and continues from tracked contracts. | Guard snippets execute in disposable repositories with the audit absent, commit-only, and commit-plus-branch. No fetch, reset, push, or merge occurs. |

The deterministic bundle regression now contains ten tests covering exact
checkpoint language, selected-subset warnings, all-payload marker scanning,
tamper/reseal controls, fresh-clone optional-input behavior, manifest and
sidecar integrity, determinism, and overwrite refusal.

## Accepted reusable-framework findings

| ID | Finding | Disposition and correction | Why it matters |
| --- | --- | --- | --- |
| UC-F2-01 | F2 omitted the owner-locked track-record dimension or merged role with authority in operator records | **Accepted with revision.** Source role, relevant track-record evidence, claim-scoped authority, support, origin/recurrence, relevance, and permission are separate. Track record is claim/domain/time/evidence scoped and may remain `UNKNOWN`; it is never a universal score. | Prevents institutional role, prior performance, and claim support from collapsing into “trusted source.” |
| UC-F6-01 | A receipt could claim `LEARNING_REVIEWED` by changing one status token | **Accepted with revision.** Reviewed learning requires a linked review, an observed outcome or explicit missing-outcome reason, and a canonical human disposition. No populated result fixture was added. | Prevents a planned or absent outcome from becoming a fabricated learning result. |
| UC-PERM-01 | Permission absence and permission uncertainty were collapsed | **Accepted with revision.** `NOT_AUTHORIZED` covers absent/revoked authorization; `UNKNOWN` covers permission not established. Both stop or escalate, but the record preserves the distinction. | Keeps access, authorization, and uncertainty inspectable. |
| UC-ROUTE-01 | `OUTCOME_REVIEW` appeared in a route column | **Accepted.** It is now explicitly an outcome-learning review artifact, not a route. The canonical route vocabulary is unchanged. | Keeps current routing separate from later learning state. |
| UC-INF-01 | Selected influence material allowed unknown permission, and recorded influence could have no selected item | **Accepted with revision.** Only `AUTHORIZED` material may be selected; unresolved permission is withheld. Recorded influence requires at least one selected item, while unrecorded influence requires none. | Makes “what shaped the answer” fail closed without equating internal influence with external disclosure permission. |
| UC-SCHEMA-01 | The JSON Schema accepted six copies of F1 | **Accepted with revision.** Draft 2020-12 `prefixItems` locks exact ordered ID/slug/name identities for F1–F6 while the shared item contract retains required family fields. | Makes the schema itself reject duplicate, reordered, renamed, incomplete, extra, and seventh-family records. |
| UC-STAGE0-01 | The fixture named ordinary actually contained layered comparison, disconfirmation, and influence work | **Accepted with revision.** The old fixture is renamed lightweight. A genuinely ordinary supplied-material fixture records `ORDINARY`, `NONE`, `ANSWER`, `COMPLETE`, assumptions, and unchecked boundaries without layered records. | Protects the framework's anti-bureaucracy Stage 0 in executable QA. |
| UC-READ-01 | The complete essay title rendered twice and the manuscript index still spoke in future tense | **Accepted.** The reader route strips only the source H1 because the surrounding section already supplies it; the canonical Markdown is unchanged. The manuscript index now describes current state. | Removes a small reader-level seam without reopening voice or thesis. |

The standard-library applied validator checks these states without adding a
runtime dependency. A separate ephemeral Draft 2020-12 engine check accepted
the current six-family document and rejected duplicate F1, reordered families,
ID/name mismatch, missing implementation level, extra fields, and a seventh
family.

## Deferred or rejected expansion

| Suggestion | Disposition | Reason |
| --- | --- | --- |
| Create a generic project-adoption brief immediately | **Deferred** | The Signal Foundry handoff already covers the demonstrated transfer. A second real project should show repeated friction before another template is added. |
| Add a conformance dictionary or universal source reputation system | **Deferred / rejected as current scope** | Neither is needed to close a reproduced defect; a universal score would violate F2. |
| Redesign the site or rewrite the essay after the outside review | **Rejected** | The broad thesis, six families, three principal doors, Guided path, Map, and planning-only Apply surface remain coherent. Contract corrections were sufficient. |
| Populate an observed-outcome fixture | **Rejected in this phase** | No authorized live outcome exists. The validator uses explicitly synthetic in-memory mutation controls, not a repository record that could be mistaken for a result. |
| Infer physical accessibility or owner comprehension from automation | **Rejected as an inference; deferred as manual review** | Source, DOM, computed geometry, and render checks cannot certify human experience. |

## Verification completed at the source checkpoint

- locked owner-intent SHA-256: pass;
- focused content-interface validator: pass;
- focused applied validator including ordinary/layered, permission, influence,
  and learning guard mutations: pass;
- ten-route site build/check and all 108 Apply planning states: pass;
- standalone structure, links, no-script, Map, terminology, and selector
  contracts: pass;
- site semantic/static audit: pass;
- Draft 2020-12 external-engine negative matrix: pass in an ephemeral tool
  environment; no dependency was added to the repository;
- portable bundle regression: 10/10 pass in 68.049 seconds;
- authored diff check: pass.

The complete twelve-stage provider-free owner-review runner must still pass on
the final evidence commit before a new packet is sealed. The final handoff may
report that exact later execution; this record does not preclaim a future run.

## Remaining honest gates

- owner and mentor comprehension, voice, and visual taste;
- physical end-to-end keyboard traversal;
- supported screen-reader review;
- real 200% browser/OS zoom;
- real forced-colors inspection;
- native browser print preview;
- hardware-touch behavior; and
- publication-time links and metadata if publication is later authorized.

No merge to `main`, deployment, publication, public-site replacement, GitHub
Release, study, participant or model experiment, provider selection/call,
external dataset acquisition, preregistration, outreach, or spend occurred.
