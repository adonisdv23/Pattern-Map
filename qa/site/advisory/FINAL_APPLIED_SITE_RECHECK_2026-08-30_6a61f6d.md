# Final applied / site recheck — exact `6a61f6d`

Status: **PASS WITH ONE P1, TWO P2, AND ONE P3 CORRECTION**

This was an independent read-only model review of exact clean checkpoint
`6a61f6da9b2c1f0255dd5d8a15e596c88b031f36`. It reviewed public copy,
generated routes, Apply contracts, publication gating, and relevant tests.
It is advisory structural evidence, not a user study, accessibility result,
publication authorization, or effectiveness result.

## Disposition-ready findings

1. **AS-R2-01 — P1, accepted.** Guided, implementation-card, and PDF copy
   could classify supplied-material work as ordinary while still requiring
   material claim judgment, comparison, selection or withholding, permission
   resolution, memory reuse, acquisition, a human action gate, or consequential
   external influence. Reuse the complete canonical Stage 0 predicate across
   those surfaces and reject the old shortcuts in cross-artifact tests.
2. **AS-R2-02 — P2, accepted.** The release host gate treated unlisted IPv6
   space and `localhost.localdomain` as public. Use positive global-unicast
   IPv6 classification, reject special/documentation allocations, add the
   localdomain suffix, and test full release configurations plus one known
   global-unicast control.
3. **AS-R2-03 — P2, accepted with revision.** The essay called software for
   consequential, contested, *or* repeated work an “advanced implementation,”
   conflicting with the formal conjunction of consequence + high uncertainty
   + substantial approved capacity. Preserve the capability point but remove
   the formal level label from that sentence.
4. **AS-R2-04 — P3, accepted.** The strict recommendation API used
   `Object.keys`, so non-enumerable own fields, symbol fields, and inherited
   fields bypassed the stated exact-record contract. Require a plain JSON-shaped
   record, use `Reflect.ownKeys`, and fail closed on those hostile cases.

## Confirmed strengths

The first-wave fixes to the ordinary/layered enum surface, outcome state
machine, F2 receipt, Advanced rule, no-script precedence, recurrence visual,
route anchor uniqueness, and current responsive compositions remained sound.
No P0, thesis rewrite, new route, live model demo, deployment, or research run
was recommended.
