# Pattern Map v16 roadmap

## Goal

Build Pattern Recognition / The Discrimination Layer v16 as a human-first
thought piece, interactive framework, and operational AI-agent playbook;
preserve v15.2 as the source checkpoint for the separate research project The
Echo Problem; and establish Pattern-Map as the canonical, reproducible project
home.

## Phase status

| Phase | Outcome | Status |
| --- | --- | --- |
| 0 — Foundation | Verified sources, immutable archive boundary, clean canonical root, lineage and binary policy | Complete — foundation commit `0c3a6e7` |
| 1 — Intent freeze | Locked owner intent, thesis/audience, artifact boundaries, acceptance criteria, v13→v16 fidelity matrix | Complete — owner-locked contracts and intent audit |
| 2 — First-wave parallel work | Echo curation, manuscript, framework/playbook, bounded advisory audits | Complete — all production lanes integrated; Wave 1, applied, continuity, and Echo findings dispositioned |
| 3 — Convergence | Integrate content, verify broad first 90 seconds and six-family continuity, freeze site interface | Complete — frozen contract and strengthened structural validator at `9079fcb`; its rendered gates were subsequently completed in Phases 4/6 within stated proxy limits |
| 4 — Site and visual system | Three-door local site, progressive definitions, map, exports, accessibility and print QA | Complete for artifact scope through convergence checkpoint `5bcd08d` — the public Home begins with the human problem and presents the three substantial doors before the teaching reveal; the current Map is line-free and responsive; F2 preserves source role, information path, track record, claim-scoped authority, support, relevance, origin, provenance, and permission; the Apply studio has one terminal ordinary state plus 144 valid layered planning states with typed permission, a separate human-action gate, and capacity mismatch handling; generated review/public standalones pass structural checks. Manual keyboard/screen-reader/real-zoom/forced-colors/print/touch confirmation remains a release residual. |
| 5 — Research containment | Echo EP v0.1/v1.1 and broader future matched-budget agenda, no study run | Complete for design scope — Tracks 01/02 remain separate; EP v1.1 adopts Amendment A1 only with the NEWS-COPY narrowing, adds targeted claim restraint, conditional power/MDE planning, strict offline checks, and a real-tokenizer parity fixture without calling a model or acquiring a corpus; both paper routes remain candidates, not results |
| 6 — Deliberate review loops | Intent/voice; comprehension/executability; research/design/accessibility/technical integrity | Final exact-integrated review loop in progress. Earlier ChatGPT Pro, Claude, intent, operator, package, live-visual, Echo, applied, and research lanes are preserved; the current convergence checkpoint combines the accepted applied, research, and public-site corrections before three new independent exact-commit red teams. No human comprehension or assistive-technology result is inferred. |
| 7 — Foundation owner-review release | Canonical artifacts, package map, checksums, pushed branches, draft PRs | Complete for the historical foundation owner-review scope. `874a0a8` is the fixed human manuscript/content-interface checkpoint, not the current operating checkout. Owner/mentor and physical keyboard/screen-reader/real-zoom/forced-colors/print/touch gates stay open; no merge/deploy |
| 8 — Public and transfer hardening | Repair applied semantic contracts, add a shared-source public mode, update the targeted 2025–2026 boundary, and prepare a local public/mentor candidate | Integrated through `5bcd08d`: the strict four-field ordinary escape, proportional layered routes, typed permission/reference/motion/current-memory contracts, separate human-action gate, capacity mismatch handling, targeted current-source boundary, orthogonal narrow-wedge axes, dual review/public renderer, substantial-door-first Home, exact F2 role/authority contract, deterministic teaching reveal, semantic heading/canonical/social-image-alt gates, two standalone exports, and classified Signal Foundry subset pass focused suites. Final exact-commit red teams, owner-manifest resealing, clean-clone verification, push/PR readback, and cross-computer ZIP sealing remain before terminal owner handoff. No thesis rewrite, generic adoption artifact, study, publication, deployment, outreach, or merge is authorized. |

## Dependency rules

- Foundation must be committed before any durable subordinate task begins.
- Intent freeze must be committed before manuscript, framework, Echo curation,
  or advisory work begins.
- Site implementation begins only after manuscript and playbook converge and
  the content interface is frozen.
- Generated image work begins only from a written visual-needs list.
- Every subordinate branch has exclusive path ownership and an isolated
  worktree. The primary orchestrator alone integrates.
- A review is not accepted because it is detailed. Every recommendation gets a
  recorded disposition against a governing requirement.

## Completed acceptance re-audits

- A07 and A08 pass the exact post-site review at `8aa5f94` and remain passing
  in the final regression check at `2a54b24`; the verdict covers artifact and
  rendered-route completeness, not live-agent behavior or effectiveness.
- A09 passes after every Signal Foundry rendered mention and cross-link is
  inspected at `2a54b24`; the case remains fixture-only, read-only, and not
  validation.
- The owner-directed site re-audit accepted the routed visual/interaction
  checkpoint through `a319794`. ChatGPT Pro then identified two deterministic
  blockers at `cc5547d`; `5eb860e` replaces the fragile connector topology and
  event-writing Apply receipt with a line-free relationship view and
  planning-only recommendation contract. Round 2 at `4d2505e` found five
  bounded P1 residues; `c889260` closes them with an explicit Stage 0 Apply
  field, accurate Home planning preview, descriptive/no-script/medium-safe term
  help, one-column mobile route briefs, and 44-pixel route targets. Ten-route,
  no-script, historical 108-combination, 320/390/480/600 mobile, 601/768/821/1024/1100
  medium, and default viewport checks pass. Physical keyboard, supported screen
  reader, real zoom, forced colors, hardware touch, browser print preview, and
  owner pacing judgment remain manual.
- The convergence review replaces the old Apply classifier with one strict
  ordinary input and 144 valid layered combinations. Disabled layered fields
  are rejected rather than silently collapsed into an ordinary state; a
  larger budget never justifies Advanced by itself, and insufficient approved
  capacity narrows or escalates high-consequence/high-uncertainty work.
- The final reports supersede the implementation-scope deferral recorded at
  the manuscript-only snapshot `ea8a6e2` without changing its historical
  report.
- The Claude package is treated as fallible advisory input. EP v1.1 accepts
  the public-corpus narrowing, graded real-world dependence, conditional
  power/MDE warning, and threshold-validation requirement; rejects a wholesale
  harness or measurement-first reframe; independently refutes Claude's claimed
  real-BPE parity fix; and corrects the replacement planning helper after a
  Luna Max audit found an invalidity-grid mismatch and unstable exact tail.
- The Signal Foundry transfer is now two explicit handoff artifacts. It reuses
  the current closed-schema `OPERATOR_DECISION` plus `RATIONALE` seam by
  default, treats `CONTEXT_DISPOSITION` as conceptual and invalid against the
  current schema, and refuses to invent the requested V14 deep link or Pattern
  Map classifier.

## Release boundary

The release in this roadmap means an owner-review branch and draft pull
request. It does not mean publication, deployment, merge, GitHub Release, or an
empirical research release.

Phase 8 may additionally produce a local public-presentation candidate and
channel-specific draft assets. Those outputs remain unpublished review
artifacts until an exact later instruction authorizes a named release action.
